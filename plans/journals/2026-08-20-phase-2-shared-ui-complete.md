---
title: Phase 2 Shared UI complete
date: 2026-08-20
summary: "Executed plans/260819-2348-phase-2-shared-ui (7/7 phases, 97/97 tasks): reconciled donor-app viewers into src/ui/ (Markdown, LessonViewer, QuizRunner, ExamEngine+review, LabViewer, Compare, Notes/LessonNotes), wired shell routing #/subject/:id/:mode[/:id] to the Content SDK with enabledModes-driven tabs, and proved the fixture pack end-to-end through every enabled mode. Key fixes: injected-assembler deep feasibility check (ValidateOptions.assemblePaper threaded createFileContentSource→assembleSubject→validateSubject, injected by src/content/registry.ts) so exclusion-starved sampled exams fail content:check instead of white-screening at sitting time; LessonNotes keyed per lesson (key={lesson.id}) fixing draft bleed across lesson hops in the mounted learn path; window-level Escape + autoFocus on the exam confirm dialog; main-region focus rescue in AppShell. Gates: 402/402 tests (44 files), oxlint clean, tsc clean, build green, content:check 3/3. Deferred: navigator-position restore on exam resume, fill-empty 'Missed' label nuance, dialog focus containment, listSubjects() fault tolerance (real-pack phase). Reports under plans/260819-2348-phase-2-shared-ui/reports/."
---

# Phase 2 Shared UI complete

Executed plans/260819-2348-phase-2-shared-ui (7/7 phases, 97/97 tasks): reconciled donor-app viewers into src/ui/ (Markdown, LessonViewer, QuizRunner, ExamEngine+review, LabViewer, Compare, Notes/LessonNotes), wired shell routing #/subject/:id/:mode[/:id] to the Content SDK with enabledModes-driven tabs, and proved the fixture pack end-to-end through every enabled mode. Key fixes: injected-assembler deep feasibility check (ValidateOptions.assemblePaper threaded createFileContentSource→assembleSubject→validateSubject, injected by src/content/registry.ts) so exclusion-starved sampled exams fail content:check instead of white-screening at sitting time; LessonNotes keyed per lesson (key={lesson.id}) fixing draft bleed across lesson hops in the mounted learn path; window-level Escape + autoFocus on the exam confirm dialog; main-region focus rescue in AppShell. Gates: 402/402 tests (44 files), oxlint clean, tsc clean, build green, content:check 3/3. Deferred: navigator-position restore on exam resume, fill-empty 'Missed' label nuance, dialog focus containment, listSubjects() fault tolerance (real-pack phase). Reports under plans/260819-2348-phase-2-shared-ui/reports/.

> Historical work record — not durable authority. Prefer docs/specs/ADRs for current decisions.
